#include "main.h"
#include <string.h>
/**
* _strlen -> returns the length of a string.
* @s: the passed string
* Return: returns te length of the string
*/

int _strlen(char *s)
{
	return (strlen(s));
}
